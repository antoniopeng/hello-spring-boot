<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.antoniopeng.hello.spring.boot.mapper.RoleMapper">
  <resultMap id="BaseResultMap" type="com.antoniopeng.hello.spring.boot.entity.Role">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="role_id" jdbcType="INTEGER" property="roleId" />
    <result column="role_name" jdbcType="VARCHAR" property="roleName" />
    <result column="role_description" jdbcType="VARCHAR" property="roleDescription" />
  </resultMap>

  <select id="selectByUserId" resultMap="BaseResultMap" parameterType="integer">
    SELECT
    `role`.role_id
    , `role`.role_name
    , `role`.role_description
    FROM
    `role`
    , `user_role`
    WHERE
    `role`.role_id = `user_role`.role_id
    AND `user_role`.user_id = #{userId }
  </select>
</mapper>